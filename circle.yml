machine:
  services:
    - docker

dependencies:
  override:
    - ./script/bootstrap

test:
  override:
    - ./script/test
    - ./script/feature

deployment:
  deploy:
    branch: master
    owner: bioboxes
    commands:
      - ./script/publish
